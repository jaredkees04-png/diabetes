import 'package:flutter/material.dart';

/// The app's brand color — also used for the app icon and PWA theme-color
/// (web/manifest.json, web/index.html), so keep those in sync if this changes.
const Color kSeedColor = Color(0xFF7C4DFF);
